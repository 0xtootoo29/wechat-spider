version: '3.8'

services:
  wechat-article-downloader:
    build: .
    container_name: wechat-article-downloader
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
