version: '3.8'

services:
  wechat-spider:
    build: .
    container_name: wechat-spider
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
